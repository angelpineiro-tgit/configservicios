spring:
  application:
    name: curso-service

server:
  port: 0

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/cursodb
    username: root
    password: root

logging:
  level:
    root: info
