spring:
  application:
    name: cursos-service

server:
  port: 0

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/cursosdb
    username: root
    password: root

logging:
  level:
    root: info
